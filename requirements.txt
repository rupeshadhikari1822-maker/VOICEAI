# --- api ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
alembic>=1.13
python-dotenv>=1.0

# --- audio analysis ---
numpy>=1.26
scipy>=1.11          # anti-aliased resampling in export_dataset.py

# --- object storage (R2 / B2 / S3 / MinIO / DigitalOcean Spaces) ---
boto3>=1.34

# --- accounts (Supabase Auth: email/password + Google sign-in) ---
# cryptography is required for ES256 -- Supabase's current JWT signing keys
# are asymmetric (ES256 via JWKS), not the legacy shared HS256 secret.
PyJWT>=2.8
cryptography>=42

# --- tests ---
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient

# --- database driver (Postgres / Supabase) ---
psycopg[binary]>=3.1

# --- optional ---
# pyloudnorm>=0.1.1     # true BS.1770 loudness for TTS export; RMS fallback if absent
#
# ASR pre-filter (scripts/asr_prefilter.py). Optional by design: without it,
# every QC-passing clip simply goes to a human and /review works unchanged.
# faster-whisper>=1.0
#
# jiwer is NOT required: CER is implemented in app/services/review/normalize.py
# so that the code deciding auto-reject is testable in the base install.
